services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=summer'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=summer'
    ports:
      - '3306:3306'
